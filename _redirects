# Netlify redirects file
# Handle client-side routing for single page applications

# Passthrough for PWA assets (must be before catch-all)
/service-worker.js   /service-worker.js   200
/app-sw.js           /app-sw.js           200
/manifest.webmanifest /manifest.webmanifest 200

# Handle workout.html as a separate page
/workout    /workout.html   200
/workout/   /workout.html   200

# Redirect all routes to index.html for client-side routing
/*    /index.html   200
